import React from 'react';
import { Box } from '../../../../components';
import * as types from '../../../../types';

type AgreeDisagreeProps = types.DiagnosisSectionProps & {
    
};

export function AgreeDisagree({}: AgreeDisagreeProps) {
    return (
        <Box>

        </Box>
    );
}
