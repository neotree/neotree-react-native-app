import React from 'react';
import { Box } from '../../../../components';
import * as types from '../../../../types';

type FullDiagnosisProps = types.DiagnosisSectionProps & {
    
};

export function FullDiagnosis({}: FullDiagnosisProps) {
    return (
        <Box>

        </Box>
    );
}
