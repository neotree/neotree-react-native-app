import React from 'react';
import { Box } from '../../../../components';
import * as types from '../../../../types';

type SortPriorityProps = types.DiagnosisSectionProps & {
    
};

export function SortPriority({}: SortPriorityProps) {
    return (
        <Box>

        </Box>
    );
}
