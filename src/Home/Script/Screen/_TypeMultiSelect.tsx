import React from 'react';
import { Box } from '../../../components';
import { useContext } from '../Context';

type TypeMultiSelectProps = {

};

export function TypeMultiSelect({}: TypeMultiSelectProps) {
    const ctx = useContext();

    return (
        <Box>

        </Box>
    );
}
