import React from 'react';
import { Box } from '../../../components';
import { useContext } from '../Context';

type TypeSingleSelectProps = {

};

export function TypeSingleSelect({}: TypeSingleSelectProps) {
    const ctx = useContext();

    return (
        <Box>

        </Box>
    );
}
