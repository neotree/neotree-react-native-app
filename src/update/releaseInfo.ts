// Bump this for OTA-only releases. Keep app.json version/versionCode fixed
// unless a new APK is being built.
export const NEOTREE_UPDATE_RELEASE = {
  baseAppVersion: '2.5.34',
  sequence: 2,
  label: '2.5.34-2',
} as const;
